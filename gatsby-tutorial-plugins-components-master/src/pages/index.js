import React from "react"
import { Link } from "gatsby"
import Layout from "../components/layout"

export default () => (
    <Layout>
        <h1>Controlled Escape</h1>
        <p>
            There is no better way to get away from it all.
            Plug in, drone on, disappear.
        </p>
    </Layout>
) 